# Ignore files that don't have a dot (no extension)
*

# Don't ignore folders
!*/

# Don't ignore files with extensions (have at least one dot not at the start)
!*.* 

# Don't ignore dotfiles like .gitignore, .env, etc.
!.*

# Don't ignore files starting with q
!q*

# Ignore mac files
*.DS_Store
